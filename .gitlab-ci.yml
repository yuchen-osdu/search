variables:
  AWS_BUILD_SUBDIR: provider/search-aws/build-aws
  AWS_TEST_SUBDIR: testing/search-test-aws
  AWS_CHART_SUBDIR: devops/aws/chart
  AWS_SERVICE: search
  AWS_SERVICE_GATEWAY: osdu-gateway
  AWS_ENVIRONMENT: dev
  AWS_DEPLOY_TARGET: TF
  AWS_EKS_DEPLOYMENT_NAME: os-search

  GCP_BUILD_SUBDIR: provider/search-gcp
  GCP_INT_TEST_SUBDIR: testing/search-test-gcp
  GCP_APPLICATION_NAME: osdu-search
  GCP_ENVIRONMENT: dev
  GCP_PROJECT: opendes
  GCP_TENANT_NAME: opendes
  GCP_DEPLOY_ENV: p4d
  GCP_DOMAIN: cloud.slb-ds.com
  GCP_STORAGE_URL: https://osdu-search-dot-opendes.appspot.com/api/storage/v2/

  AZURE_SERVICE: search
  AZURE_BUILD_SUBDIR: provider/search-azure
  AZURE_TEST_SUBDIR: testing/search-test-azure
  SERVICE_JAVA_VERSION: "17"
  ACCEPTANCE_TEST_DIR: "search-acceptance-test"

  CIMPL_OPENAPI_SPEC_PATH: docs/api/community/v2/openapi.yaml
  CIMPL_OPENAPI_YAML_ROUTE: /api/search/v2/api-docs.yaml

  CORE_BUILD_SUBDIR: search-core

include:
  - project: "osdu/platform/ci-cd-pipelines"
    file: "standard-setup.yml"

  - project: "osdu/platform/ci-cd-pipelines"
    file: "build/maven.yml"

  - project: "osdu/platform/ci-cd-pipelines"
    file: "scanners/fossa-maven.yml"

  - project: "osdu/platform/ci-cd-pipelines"
    file: "scanners/gitlab-ultimate.yml"

  - project: "osdu/platform/ci-cd-pipelines"
    file: "reporting/reporting.yml"

  - project: "osdu/platform/ci-cd-pipelines"
    file: "publishing/common-pages.yml"

  - project: "osdu/platform/ci-cd-pipelines"
    file: "cloud-providers/aws-global.yml"
 
  - project: "osdu/platform/ci-cd-pipelines"
    file: "cloud-providers/aws-maven.yml"

  - project: "osdu/platform/ci-cd-pipelines"
    file: "cloud-providers/azure.yml"

  - project: "osdu/platform/ci-cd-pipelines"
    file: "cloud-providers/gc-global.yml"

  - project: "osdu/platform/ci-cd-pipelines"
    file: "cloud-providers/cimpl-global.yml"

  - local: "devops/core-plus/pipeline/override-stages.yml"
  - project: "osdu/platform/ci-cd-pipelines"
    file: "cloud-providers/aws-one-pipeline.yml"

  - project: "osdu/platform/ci-cd-pipelines"
    file: "cloud-providers/azure-one-pipeline.yml"

  - project: "osdu/platform/ci-cd-pipelines"
    file: "cloud-providers/gc-one-pipeline.yml"

  - local: "devops/gc/pipeline/override-stages.yml"
  - local: "devops/aws/pipeline/override-stages.yml"

download_plugins:
  image: maven:3.8.3-openjdk-17-slim
  stage: build
  variables:
    APD_PACKAGE_REGISTRY_URL: "https://community.opengroup.org/api/v4/projects/1445/packages/maven"
    APD_VERSION: "0.26.0"

  artifacts:
    paths:
      - ./tmp/*.jar
    when: always
    expire_in: 1 days

  script:
    - mvn dependency:copy -DrepoUrl=$APD_PACKAGE_REGISTRY_URL -Dartifact="org.opengroup.osdu:apd-openid:$APD_VERSION:jar:plugin" -Dtransitive=false -DoutputDirectory="./tmp"

  only:
    variables:
      - $PROTECTED == '1'

aws-test-java:
  image: $CI_REGISTRY/osdu/platform/deployment-and-operations/base-containers-aws/aws-maven/aws-maven:v2.0

schemathesis-tests:
  stage: acceptance
  image:
    name: community.opengroup.org:5555/osdu/platform/system/sdks/schemathesis-base:latest
    entrypoint: [""]
  needs:
    - cimpl-deploy
  extends: .cimpl-variables
  variables:
    GIT_STRATEGY: none
    KEYCLOAK_ENDPOINT: ${TEST_OPENID_PROVIDER_URL}/protocol/openid-connect/token
    CLIENT_ID: ${TEST_OPENID_PROVIDER_CLIENT_ID}
    CLIENT_SECRET: ${TEST_OPENID_PROVIDER_CLIENT_SECRET}
  script:
    - |
      cd /app

      SCHEMA_LOCATION="https://osdu.${CIMPL_DOMAIN}${CIMPL_OPENAPI_YAML_ROUTE}"
      echo "KEYCLOAK_ENDPOINT: $KEYCLOAK_ENDPOINT"
      echo "CLIENT_ID is set: ${CLIENT_ID:+yes}"
      echo "CLIENT_SECRET is set: ${CLIENT_SECRET:+yes}"
      echo "Schema location: $SCHEMA_LOCATION"

      TEST_EXIT_CODE=0

      uv run run_schemathesis.py "$SCHEMA_LOCATION" || TEST_EXIT_CODE=$?

      allure generate --single-file output/allure/results -o output/report --clean || echo "Allure report generation failed"

      exit "$TEST_EXIT_CODE"
  artifacts:
    when: always
    expose_as: "Allure Report"
    reports:
      junit: /app/output/junit/junit.xml
    paths:
      - /app/output/report/index.html
  only:
    variables:
      - $PROTECTED == '1'
